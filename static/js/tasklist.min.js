"use strict";import React from"react";import ReactDOM from"react-dom";import{describeAge}from"agetext";import{NewRequest}from"newrequest";import{NOT_MODIFIED,PollCache}from"pollcache";function debug_log(...args){// uncomment for debugging in development
// console.log(...args);
}// How often each thing is polled.
//
// The full task list used to be re-fetched every 2 seconds. Most of what a waiting user is watching
// is their queue position, and queuepositions.json answers that with two indexed queries and a few
// hundred bytes instead of a page of serialised tasks, so that is what runs on the short interval.
// A task dropping out of the queue positions response means it has finished, which triggers an
// immediate full fetch, so "my task is done" still appears within one short interval.
const TASKLIST_POLL_MS=6000;const QUEUEPOS_POLL_MS=2000;const RUNNERSTATUS_POLL_MS=60000;const SITE_TITLE="ATLAS Forced Photometry";function pageTitle(taskid){// matches the server-rendered <title>, so a client-side navigation does not leave the tab
// claiming to show something else
return(taskid!=null?"Task "+taskid:"Task Queue")+" \u2013 "+SITE_TITLE}function pollingPaused(){return document[hidden]||!user_is_active}/**
 * Run fn every ms milliseconds, skipping the ticks where polling is paused.
 *
 * The pause belongs to the repeating polls and to nothing else, so it is applied here, at the
 * only place that repeats. It used to sit inside the functions being polled, where it also
 * caught the many callers that are not polls at all — a click on a task, a filter, a page, a
 * delete, a history navigation — and silently dropped them, leaving the URL and the heading on
 * the new page while the tasks on screen stayed those of the old one, with nothing to correct it
 * until the mouse moved again (two minutes without a mousemove counts as "away").
 */function pollInterval(fn,ms){return setInterval(()=>{if(!pollingPaused()){fn()}},ms)}class TaskPlot extends React.PureComponent{constructor(props){super(props)}componentDidMount(){debug_log("activating plot",this.props.taskid);const plot_url=new URL(this.props.taskurl);plot_url.pathname+="resultplotdata.js";plot_url.search="";$.ajax({url:plot_url,cache:true,dataType:"script"})}componentWillUnmount(){debug_log("Unmounting plot for task ",this.props.taskid);delete jslimitsglobal["#plotforcedflux-task-"+this.props.taskid];delete jslcdataglobal["#plotforcedflux-task-"+this.props.taskid];delete jslabelsglobal["#plotforcedflux-task-"+this.props.taskid]}render(){return/*#__PURE__*/React.createElement("div",{key:"plot",id:"plotforcedflux-task-"+this.props.taskid,className:"plot",style:{width:"100%",height:"300px"}})}}export class Task extends React.Component{constructor(props){super(props);this.state={};this.state.updateTimeElapsed=this.updateTimeElapsed.bind(this);this.state.interval=null;this.state.timeelapsed=-1;this.state.httperror=""}deleteTask(){const task=this.props.taskdata;// deleting a finished task also removes its data file, plot and any retrieved images, and
// none of that can be recovered. Cancelling a task that has not finished stays one click.
if(task.finishtimestamp!=null&&!window.confirm("Delete task "+task.id+"? Its data file, plot and any retrieved images will be"+" removed and cannot be recovered.")){return}const li_id="#task-"+task.id;// $(li_id).hide(300);
$(li_id).slideUp(200);setTimeout(()=>{$.ajax({headers:{"X-CSRFToken":getCookie("csrftoken")},url:task.url,method:"delete",// not fetchData(true): "user triggered" re-applies the cached pre-delete body and
// scrolls the window to the top, which after deleting the last row on a page means
// the viewport jumps away from what the user was looking at
success:result=>{console.log("Deleted task ",task.id);this.setState({"httperror":""});this.props.fetchData()},error:err=>{// the row slides back, so without a message the click looks like it simply did
// nothing. A 403 here means the task belongs to somebody else; a 401 means the
// session has ended, so reload the page, which lands on the login form.
console.log("Failed to delete task ",task.id,err);if(err.status===401){window.location.reload();return}$("#task-"+task.id).slideDown(100);// jQuery reports a network-level failure as status 0, which is not an HTTP
// status and reads as gibberish in a message
let message="ERROR: could not delete this task (HTTP "+err.status+").";if(err.status===403){message="ERROR: you are not allowed to delete this task."}else if(err.status===0){message="ERROR: could not reach the server to delete this task."}this.setState({"httperror":message});this.props.fetchData()}})},200)}requestImages(){const request_image_url=new URL(this.props.taskdata.url);// the trailing slash matters: without it APPEND_SLASH answers with a 301, and a browser
// retries a redirected POST as a GET, which this endpoint does not accept
request_image_url.pathname+="requestimages/";request_image_url.search="";fetch(request_image_url,{credentials:"same-origin",method:"POST",headers:{"X-CSRFToken":getCookie("csrftoken"),"Accept":"application/json","Content-Type":"application/json"}}).then(response=>{if(response.status==200&&response.redirected){this.setState({"httperror":""});const newimgtask_id=parseInt(new URL(response.url).searchParams.get("newids"));newtaskids.push(newimgtask_id);debug_log("requestimages created task",newimgtask_id);const new_page_url=new URL(response.url);new_page_url.searchParams.delete("newids");window.history.pushState({},document.title,new_page_url);this.props.fetchData(true)}else{// the body is not always JSON (e.g. a plain-text 404), and DRF reports its
// own errors under 'detail' rather than 'non_field_errors'
return response.text().then(text=>{let message=response.statusText||"HTTP "+response.status;try{const data=JSON.parse(text);message=data["non_field_errors"]||data["detail"]||message}catch(err){console.log("requestImages: non-JSON error body",text)}console.log("requestImages: error returned",response.status,message);this.setState({"httperror":"ERROR: "+message})})}return null}).catch(error=>{console.log("requestImages HTTP request failed",error);this.setState({"httperror":"HTTP request failed."})})}static getDerivedStateFromProps(props,state){if(props.taskdata.starttimestamp!=null&&props.taskdata.finishtimestamp==null){if(state.interval==null){const starttime=new Date(props.taskdata.starttimestamp).getTime();const timeelapsed=(new Date().getTime()-starttime)/1000;return{"interval":setInterval(state.updateTimeElapsed,1000),"timeelapsed":timeelapsed.toFixed(0)}}}else if(state.interval!=null){// the task is no longer running, so stop the timer. It must be cleared here:
// once state.interval is null, updateTimeElapsed() can no longer find it.
clearInterval(state.interval);return{"interval":null}}return null}componentDidMount(){// componentDidUpdate() {
this.updateTimeElapsed();if(newtaskids.includes(this.props.taskdata.id)){const li_id="#task-"+this.props.taskdata.id;debug_log("showing new task",this.props.taskdata.id);$(li_id).hide();// $(li_id).show(600);
$(li_id).slideDown(200);newtaskids=newtaskids.filter(item=>{return item!==this.props.taskdata.id})}// this.interval = setInterval(() => {this.updateTimeElapsed()}, 1000);
}componentWillUnmount(){clearInterval(this.state.interval);// this.state.interval = null;
}updateTimeElapsed(){if(this.props.taskdata.starttimestamp!=null&&this.props.taskdata.finishtimestamp==null){const starttime=new Date(this.props.taskdata.starttimestamp).getTime();const timeelapsed=(new Date().getTime()-starttime)/1000;this.setState({"timeelapsed":timeelapsed.toFixed(0)})}else if(this.state.interval!=null){clearInterval(this.state.interval);this.setState({"interval":null})}}shouldComponentUpdate(nextProps,nextState){if(nextState.httperror!=this.state.httperror){return true}if(nextProps.taskdata.starttimestamp!=null&&nextProps.taskdata.finishtimestamp==null){return true}if(JSON.stringify(nextProps)!=JSON.stringify(this.props)){return true}return false}render(){const task=this.props.taskdata;// whether the data file this task produced is still on disk. The maintenance sweep
// reclaims it after a few months, and the serializer answers with a null result_url from
// then on; everything derived from that file — the download links, the plot, and the
// image request that reads it to know which observations to fetch — turns on this.
const hasresultfile=task.result_url!=null;let statusclass="none";let buttontext="none";if(task.finishtimestamp!=null){statusclass="finished";buttontext="Delete"}else if(task.starttimestamp!=null){statusclass="queued started";buttontext="Cancel"}else{statusclass="queued notstarted";buttontext="Cancel"}debug_log("Task "+task.id+" rendered");let delbutton=null;if(task.user_id==user_id){delbutton=/*#__PURE__*/React.createElement("button",{className:"btn btn-sm btn-danger",onClick:()=>this.deleteTask()},buttontext)}// rendered only when there is one: React drops a null src, which left an empty <img> box in
// every row that has no preview, and without alt text there was nothing to announce either
const previewimage=task.previewimage_url?/*#__PURE__*/React.createElement("img",{className:"previewimage",src:task.previewimage_url,height:"100",loading:"lazy",alt:"Preview image for task "+task.id}):null;let taskbox=[/*#__PURE__*/React.createElement("div",{key:"rightside",className:"rightside"},delbutton,previewimage)];taskbox.push(/*#__PURE__*/React.createElement("div",{key:"tasknum"},/*#__PURE__*/React.createElement("a",{key:"tasklink",href:task.url,onClick:e=>{this.props.setSingleTaskView(e,task.id,task.url)}},"Task ",task.id)));if(task.parent_task_url){taskbox.push(/*#__PURE__*/React.createElement("p",{key:"imgrequest"},"Image request for ",/*#__PURE__*/React.createElement("a",{key:"parent_task_link",href:task.parent_task_url,onClick:e=>{this.props.setSingleTaskView(e,task.parent_task_id,task.parent_task_url)}},"Task ",task.parent_task_id)))}else if(task.parent_task_id){taskbox.push(/*#__PURE__*/React.createElement("p",{key:"imgrequest"},"Image request for Task ",task.parent_task_id," (deleted)"))}else if(task.request_type=="IMGZIP"){taskbox.push(/*#__PURE__*/React.createElement("p",{key:"imgrequest"},"Image request"))}if(task.request_type=="IMGZIP"){const imagetype=task.use_reduced?"reduced":"difference";taskbox.push(/*#__PURE__*/React.createElement("p",{key:"imgrequestnote"},"Up to the first 1000 ",imagetype," images will be retrieved. The image request and download link may expire after one week."))}if(task.user_id!=user_id){taskbox.push(/*#__PURE__*/React.createElement("div",{key:"user"},"User: ",task.username))}if(task.comment!=null&&task.comment!=""){taskbox.push(/*#__PURE__*/React.createElement("div",{key:"comment"},"Comment: ",/*#__PURE__*/React.createElement("b",null,task.comment)))}if(task.mpc_name!=null&&task.mpc_name!=""){taskbox.push(/*#__PURE__*/React.createElement("div",{key:"target"},"MPC Object: ",task.mpc_name))}else{let radecepoch="";if(task.radec_epoch_year!=null){radecepoch=/*#__PURE__*/React.createElement("span",null,"(epoch ",task.radec_epoch_year,") ")}taskbox.push(/*#__PURE__*/React.createElement("div",{key:"target"},"RA Dec: ",radecepoch,task.ra," ",task.dec));// proper motion components are signed, so testing for > 0 hides half of all real values
if(task.propermotion_ra!=null&&task.propermotion_ra!=0||task.propermotion_dec!=null&&task.propermotion_dec!=0){taskbox.push(/*#__PURE__*/React.createElement("div",{key:"propermotion"},"Proper motion [mas/yr]: ",task.propermotion_ra," ",task.propermotion_dec))}}if(task.request_type=="SSOSTACK"){taskbox.push(/*#__PURE__*/React.createElement("div",{key:"imgtype"},"Image: Stacked"))}else{taskbox.push(/*#__PURE__*/React.createElement("div",{key:"imgtype"},"Images: ",task.use_reduced?"Reduced":"Difference"))}if(task.mjd_min!=null||task.mjd_max!=null){const mjdmin=task.mjd_min!=null?task.mjd_min:"0";const mjdmax=task.mjd_max!=null?task.mjd_max:"\u221E";taskbox.push(/*#__PURE__*/React.createElement("div",{key:"mjdrange"},"MJD request: [",mjdmin,", ",mjdmax,"]"))}taskbox.push(/*#__PURE__*/React.createElement("div",{key:"queuetime"},"Queued at ",new Date(task.timestamp).toLocaleString()));if(task.finishtimestamp!=null){taskbox.push(/*#__PURE__*/React.createElement("div",{key:"status"},"Finished at ",new Date(task.finishtimestamp).toLocaleString()));if(task.error_msg!=null){taskbox.push(/*#__PURE__*/React.createElement("p",{key:"error_msg",className:"taskerror"},"Error: ",task.error_msg))}else{// React drops a null href, so before hasresultfile was checked here these rendered
// as buttons that looked live but did nothing at all when clicked.
const resultsexpired=/*#__PURE__*/React.createElement("p",{key:"expired"},"The download link has expired. Delete this task and request again if necessary.");if(task.request_type=="FP"){if(hasresultfile){taskbox.push(/*#__PURE__*/React.createElement("a",{key:"datalink",className:"results btn btn-info getdata",href:task.result_url,target:"_blank",rel:"noopener"},"Data"));taskbox.push(/*#__PURE__*/React.createElement("a",{key:"pdflink",className:"results btn btn-info getpdf",href:task.pdfplot_url,target:"_blank",rel:"noopener"},"PDF"))}else{taskbox.push(resultsexpired)}}else if(task.request_type=="SSOSTACK"){if(hasresultfile){taskbox.push(/*#__PURE__*/React.createElement("a",{key:"datalink",className:"results btn btn-info getdata",href:task.result_url,target:"_blank",rel:"noopener"},"Data"))}if(task.result_imagestack_url!=null){taskbox.push(/*#__PURE__*/React.createElement("a",{key:"imgdownload",className:"results btn btn-info",href:task.result_imagestack_url,target:"_blank",rel:"noopener"},"Stacked image (FITS)"))}else{taskbox.push(resultsexpired)}}if(task.request_type=="IMGZIP"){if(task.result_imagezip_url!=null){taskbox.push(/*#__PURE__*/React.createElement("a",{key:"imgdownload",className:"results btn btn-info",href:task.result_imagezip_url},"Download images (ZIP)"))}else{taskbox.push(resultsexpired)}}else if(task.imagerequest_task_id!=null){if(task.imagerequest_finished){taskbox.push(/*#__PURE__*/React.createElement("a",{key:"imgrequest",className:"btn btn-primary",href:task.imagerequest_url,onClick:e=>{this.props.setSingleTaskView(e,task.imagerequest_task_id,task.imagerequest_url)}},"Images retrieved"))}else{taskbox.push(/*#__PURE__*/React.createElement("a",{key:"imgrequest",className:"btn btn-warning",href:task.imagerequest_url,onClick:e=>{this.props.setSingleTaskView(e,task.imagerequest_task_id,task.imagerequest_url)}},"Images requested"))}}else if(task.request_type=="FP"&&user_id==task.user_id&&hasresultfile){// hasresultfile: without that file this could only queue a job certain to fail
taskbox.push(/*#__PURE__*/React.createElement("button",{key:"imgrequest",className:"btn btn-info",onClick:()=>this.requestImages(),title:"Download FITS and JPEG images for up to the first 1000 observations."},"Request ",task.use_reduced?"reduced":"diff"," images"))}}}else if(task.starttimestamp!=null){taskbox.push(/*#__PURE__*/React.createElement("div",{key:"status",className:"taskstatus running"},"Running (started ",this.state.timeelapsed," seconds ago)"))}else if(task.queuepos!=null){const tasksahead=task.queuepos==1?"1 task":task.queuepos+" tasks";taskbox.push(/*#__PURE__*/React.createElement("div",{key:"status",className:"taskstatus waiting"},"Waiting (",tasksahead," ahead of this one)"))}else{// queuepos is null until the queue has been renumbered, and the sentence used to be
// rendered with the number simply missing: "Waiting ( tasks ahead of this one)"
taskbox.push(/*#__PURE__*/React.createElement("div",{key:"status",className:"taskstatus waiting"},"Waiting in queue"))}if(this.state.httperror!=""){taskbox.push(/*#__PURE__*/React.createElement("p",{key:"httperror",className:"errors",role:"alert"},this.state.httperror))}// hasresultfile because there is nothing to plot once the data file has been reclaimed.
// Without it, every expired task reserved a 300px-tall empty box in its row and fetched
// a resultplotdata.js that the server answers with an empty body.
if(task.finishtimestamp!=null&&task.error_msg==null&&task.request_type=="FP"&&hasresultfile&&!this.props.hidePlot){taskbox.push(/*#__PURE__*/React.createElement(TaskPlot,{key:"plot",taskid:task.id,taskurl:task.url}))}return/*#__PURE__*/React.createElement("li",{key:"task-"+task.id,className:"task "+statusclass,id:"task-"+task.id},taskbox)}}let tasklist_api_request_active=false;// set when a refresh was requested while a request was already in flight; the settled request
// re-fetches so the refresh is delayed rather than lost
let tasklist_refresh_queued=false;const tasklist_fetchcache={};// remembers the ETag of each polled page so that an unchanged page can be answered with a 304
const tasklist_pollcache=new PollCache;class RunnerStatus extends React.PureComponent{constructor(props){super(props);this.state={status:null};this.fetchStatus=this.fetchStatus.bind(this)}componentDidMount(){// the mount fetch runs whether or not polling is paused, for the same reason fetchData's
// does: it is the one that fills the page, not a repeat of it. That also retires the
// 2-second retry timer this used to need — the retry existed only because a tab opened in
// the background (cmd-click, session restore) had its mount fetch dropped and would
// otherwise have shown no outage banner until the 60-second interval first fired.
this.fetchStatus();this.interval=pollInterval(this.fetchStatus,RUNNERSTATUS_POLL_MS)}componentWillUnmount(){clearInterval(this.interval)}fetchStatus(){// a stale runner is reported with HTTP 503 and a body, so the status is read from the body
// rather than from the status code
fetch(taskrunnerstatus_url,{credentials:"same-origin",headers:{"Accept":"application/json"}}).then(response=>response.json()).then(status=>{this.lastgoodfetch=Date.now();this.setState({"status":status})}).catch(error=>{// not being able to reach this endpoint says nothing about the runner, so do not
// claim an outage on the strength of our own failed request — but a status we have
// not been able to re-confirm for several polls is no longer worth asserting
// either, in either direction: a frozen "3 slots busy" line outlives a dead
// runner, and a frozen outage banner outlives a recovered one.
debug_log("Could not read the task runner status",error);if(this.lastgoodfetch!=null&&Date.now()-this.lastgoodfetch>RUNNERSTATUS_POLL_MS*5){this.setState({"status":null})}})}render(){const status=this.state.status;if(status==null){return null}if(status.stale){const age=status.status_age_seconds!=null?" It last reported "+describeAge(status.status_age_seconds)+".":"";return/*#__PURE__*/React.createElement("p",{key:"runnerstatus",id:"runnerstatus",className:"runnerstatus stale",role:"status"},"The task runner is not currently processing jobs.",age," Queued tasks will start once it is back; there is no need to submit them again.")}if(!status.queued_task_count){// an idle runner with an empty queue is the normal case and needs no commentary
return null}// during the hourly maintenance sweep the slot counts are frozen (nothing is dispatched or
// reaped while it runs), so say what is happening rather than reporting numbers that are
// temporarily meaningless
const activity=status.maintenance?"maintenance sweep in progress;":status.slots_busy+" of "+status.numslots+" slots busy,";return/*#__PURE__*/React.createElement("p",{key:"runnerstatus",id:"runnerstatus",className:"runnerstatus",role:"status"},"Task runner: ",activity," ",status.queued_task_count," unfinished ",status.queued_task_count==1?"task":"tasks"," from all users in the queue.")}}class Pager extends React.PureComponent{constructor(props){super(props);this.state={};if(this.props.previous!=null){this.state.previous_cursor=new URL(this.props.previous).searchParams.get("cursor")}if(this.props.next!=null){this.state.next_cursor=new URL(this.props.next).searchParams.get("cursor")}}// componentWillReceiveProps(nextProps) {
//   if (JSON.stringify(nextProps) != JSON.stringify(this.state))
//   {
//     this.setState(nextProps);
//     console.log('Pager changed');
//   }
// }
static getDerivedStateFromProps(props,state){const statechanges={};if(props.previous!=null){statechanges.previous_cursor=new URL(props.previous).searchParams.get("cursor")}if(props.next!=null){statechanges.next_cursor=new URL(props.next).searchParams.get("cursor")}return statechanges}render(){debug_log("Pager rendered");if(this.props.taskcount==null){return null}else{return/*#__PURE__*/React.createElement("div",{id:"paginator",key:"paginator"},/*#__PURE__*/React.createElement("p",{key:"pagedescription"},"Showing tasks ",this.props.pagefirsttaskposition+1,"-",this.props.pagefirsttaskposition+this.props.pagetaskcount," of ",this.props.taskcount),/*#__PURE__*/React.createElement("ul",{key:"prevnext",className:"pager"},this.props.previous!=null?/*#__PURE__*/React.createElement("li",{key:"previous",className:"previous"},/*#__PURE__*/React.createElement("button",{type:"button",onClick:()=>{this.props.updateCursor(this.state.previous_cursor)}},"\xAB Newer")):null,this.props.next!=null?/*#__PURE__*/React.createElement("li",{key:"next",className:"next"},/*#__PURE__*/React.createElement("button",{type:"button",onClick:()=>{this.props.updateCursor(this.state.next_cursor)}},"Older \xBB")):null))}}}export class TaskPage extends React.Component{constructor(props){super(props);this.state={taskcount:null,results:null,scrollToTopAfterUpdate:false,dataurl:window.location.href,fetchtimeelapsed:null,// component state, not a module variable: setting a module variable from the failure
// handler changed nothing on screen, because nothing re-rendered. By the time a later
// poll did render, the variable had already been cleared, so a connection problem was
// never actually shown to anyone.
tasklist_api_error:""};this.newRequest=/*#__PURE__*/React.createRef();this.setSingleTaskView=this.setSingleTaskView.bind(this);this.updateCursor=this.updateCursor.bind(this);this.fetchData=this.fetchData.bind(this);this.fetchQueuePositions=this.fetchQueuePositions.bind(this);this.handlePopState=this.handlePopState.bind(this)}/**
     * Re-read the URL after the browser moved through history.
     *
     * Every client-side navigation here (a task, a filter, a page) is a pushState, so Back and
     * Forward change the URL without React hearing about it. dataurl decides the heading, the
     * active filter and whether the new request form is shown, so leaving it behind showed the
     * wrong page entirely.
     */handlePopState(){debug_log("History navigation to",window.location.href);// No guard comparing state.dataurl against the new URL: dataurl is written by
// fetchData's asynchronous setState, so a quick Back could find it still equal to the
// destination and be dropped entirely. A redundant fetch on a spurious popstate is
// harmless by comparison.
//
// The pagination fields are nulled the way setSingleTaskView() nulls them (the Pager
// only hides itself when taskcount is null): a Forward onto a task detail page that is
// not held in the cache would otherwise keep the list's "Showing tasks 1-6 of N"
// paginator, with live page buttons, above a single task until the response lands.
//
// fetchData(true, false): user-triggered, so the held copy of the destination page
// (when there is one) goes up straight away — but no scroll to top, because for a
// history navigation the browser restores the previous scroll position itself, and
// Back from a task should land on the list row the user came from.
this.setState({next:null,previous:null,pagefirsttaskposition:null,taskcount:null},()=>{this.fetchData(true,false)})}filterIsActive(filtername,strurl){const started=new URL(strurl).searchParams.get("started");if(filtername==null){return started==null&&this.singleTaskViewTaskId(this.state.dataurl)==null}return filtername=="started"&&started=="true"}filterclass(filtername,strurl){return this.filterIsActive(filtername,strurl)?"btn-primary":"btn-link"}setFilter(filtername){debug_log("changed filter to",filtername);const new_page_url=new URL(api_url_base);new_page_url.search="";if(filtername!=null){new_page_url.searchParams.set(filtername,true)}if(new_page_url!=window.location.href){window.history.pushState({},document.title,new_page_url);const statechanges={"scrollToTopAfterUpdate":true,dataurl:new_page_url};if(filtername=="started"&&this.state.results!=null){statechanges["results"]=this.state.results.filter(task=>{return task.starttimestamp!=null});if(statechanges["results"].length==0){// prevent flash of "there are no results" for empty ([] non-null) results list
statechanges["results"]=null}}this.setState(statechanges,()=>{this.fetchData(true)})}}singleTaskViewTaskId(strurl){const pathext=strurl.toString().replace(api_url_base.toString(),"").split("/").filter(el=>{return el.length!=0});if(pathext.length==1&&!isNaN(pathext[0])){return parseInt(pathext[0])}else{return null}}setSingleTaskView(event,task_id,task_url){if(event.ctrlKey||event.metaKey||event.shiftKey){return;// let the browser deal with the click natively
}event.preventDefault();const new_page_url=api_url_base+task_id+"/";window.history.pushState({},document.title,new_page_url);debug_log("Task list changed to single task view for ",new_page_url.toString());let newresults=this.state.results.filter(task=>{return task.id==task_id});if(newresults.length==0){newresults=null;// prevent flash of "there are no results" for empty (non-null) results list
}this.setState({results:newresults,scrollToTopAfterUpdate:true,next:null,previous:null,pagefirsttaskposition:null,taskcount:null},()=>{this.fetchData(true)})}updateCursor(new_cursor){if(new_cursor==new URL(window.location.href).searchParams.get("cursor")){return}debug_log("Task list cursor changed to ",new_cursor);const new_page_url=new URL(window.location.href);if(new_cursor!=null){new_page_url.searchParams.set("cursor",new_cursor)}else{new_page_url.searchParams.delete("cursor")}new_page_url.searchParams.delete("format");window.history.pushState({},document.title,new_page_url);this.setState({scrollToTopAfterUpdate:true},()=>{this.fetchData(true)})}/**
     * Refresh just the queue positions of this user's unfinished tasks.
     *
     * Cheap enough to run on the short interval: two indexed queries and a few hundred bytes,
     * against a page fetch, a prefetch and a full serialisation for the task list. A task that is
     * no longer listed here has finished (or been deleted), which is the transition worth reacting
     * to immediately, so that case falls back to a full fetch straight away.
     */fetchQueuePositions(){// no pollingPaused() check: see pollInterval()
if(this.state.results==null){return}// only this user's own tasks appear in the response, so a staff member looking at somebody
// else's task must not be counted as "no longer queued". A task whose position has not been
// assigned yet is not in the response either, and counting it would fire a full fetch on
// every tick, which is worse than not having this endpoint at all.
const trackable=task=>task.user_id==user_id&&task.finishtimestamp==null&&task.queuepos!=null;if(!this.state.results.some(trackable)){return}fetch(queuepositions_url,{credentials:"same-origin",headers:{"Accept":"application/json"},cache:"no-store"}).then(response=>response.status==200?response.json():null).then(data=>{if(data==null||data.queuepositions==null||this.state.results==null){return}// tested against state as it stands rather than the committed state: a false
// positive costs one extra full fetch and a false negative is caught on the next
// tick, so it does not need to be exact
if(this.state.results.some(task=>trackable(task)&&!(String(task.id)in data.queuepositions))){debug_log("a task left the queue: fetching the full task list");// the pause is re-checked because it can have begun during this request's
// round-trip, and the full fetch is exactly the work it exists to skip.
// Nothing is lost: the next unpaused tick repeats this comparison.
if(!pollingPaused()){this.fetchData(false)}return}const geturl=window.location.href;// functional, because the two polls are phase-locked (6000 is a multiple of 2000):
// a task list response landing in the same batch would otherwise be overwritten
// with the rows this request was started with, and its ETag has already been
// recorded, so the next poll would 304 and never resend them.
//
// tasklist_last_fetch_time and tasklist_api_error are deliberately NOT touched
// here: this endpoint says nothing about whether the task list is reachable, and
// stamping them would report a stale page as current.
this.setState(prevstate=>{if(prevstate.results==null){return null}let changed=false;const newresults=prevstate.results.map(task=>{const queuepos=data.queuepositions[String(task.id)];if(queuepos===undefined||queuepos===task.queuepos||task.finishtimestamp!=null){return task}changed=true;return{...task,queuepos:queuepos}});return changed?{results:newresults}:null},()=>{// the caches have to move with the state, or a user-triggered fetch (a filter,
// the pager, a delete) re-applies the body held here and visibly rewinds the
// positions that were just corrected
const cached=tasklist_fetchcache[geturl];if(cached!=null&&cached.results!=null&&geturl==window.location.href){const patched={...cached,results:this.state.results};tasklist_fetchcache[geturl]=patched;tasklist_pollcache.storeBody(geturl,patched)}})}).catch(error=>{// the full task list poll is what reports a connection problem; a failure here just
// means the positions are refreshed a few seconds later than they might have been
debug_log("Queue positions request failed",error)})}fetchData(usertriggered,scrolltotop=usertriggered){// no pollingPaused() check here either, for the reason given on pollInterval().
// scrolltotop is separate from usertriggered because of history navigations: they are
// user-triggered (the held copy of the destination page must apply immediately), but
// the browser restores the old scroll position itself and a scroll to top would fight it.
this.setState({dataurl:window.location.href});// start by applying a cached version if we have it
// then send out an HTTP request and update when available
if(usertriggered){const tasklist_fetchcachematch=window.location.href in tasklist_fetchcache;if(tasklist_fetchcachematch){debug_log("using tasklist_fetchcache before GET response",window.location.href);this.setState(tasklist_fetchcache[window.location.href])}else{debug_log("no tasklist_fetchcache for",window.location.href)}}if(tasklist_api_request_active&&!usertriggered){// queued rather than dropped: a refresh requested here (e.g. by deleteTask, whose
// response raced the poll) would otherwise be swallowed, and the in-flight response
// was serialised before whatever prompted it, so its data is already stale
debug_log("queueing refresh behind the in-flight GET request");tasklist_refresh_queued=true;return}tasklist_api_request_active=true;const get_url=window.location.href;debug_log("Fetching task list from",get_url);const request_headers=tasklist_pollcache.requestHeaders(get_url,{"X-CSRFToken":getCookie("csrftoken"),"Accept":"application/json","Content-Type":"application/json"});fetch(get_url,{credentials:"same-origin",method:"GET",headers:request_headers,// the browser HTTP cache would answer from its own copy and hide the 304 handling
cache:"no-store",redirect:"manual"}).then(response=>{tasklist_api_request_active=false;if(tasklist_pollcache.noteResponse(get_url,response.status,response.headers.get("ETag"))===NOT_MODIFIED){debug_log("Task list unchanged (304)",get_url);this.setState({tasklist_api_error:""});return NOT_MODIFIED}if(response.type==="opaqueredirect"){// redirect to login page
window.location.href=response.url;console.log("Fetch got a redirection to ",response.url)}else{if(response.status!=200){console.log("Fetch received HTTP status ",response.status)}if(response.status==401||response.status==403){// the session has gone (expired, or logged out in another tab). The server
// answers a JSON request with a real status rather than a redirect, so
// nothing navigates on our behalf; without this the page would sit here
// showing the pre-logout task list forever, with no error and no way back.
this.setState({tasklist_api_error:"Your session has ended. Reloading to sign in again\u2026"});window.location.reload();return null}if(response.status==404){// a handled case (the viewed task was deleted), not a server error: without
// this return it fell through to the message below and flashed
// "Server error (HTTP 404)" during a perfectly normal navigation
window.history.pushState({},document.title,api_url_base);this.setState({scrollToTopAfterUpdate:true},()=>{this.fetchData(true)});return null}if(response.status==200){this.setState({tasklist_api_error:""});return response.json()}// cleared only for a response we could actually use: clearing it up front meant
// a 500 on every poll left the page looking healthy and merely frozen
this.setState({tasklist_api_error:"Server error (HTTP "+response.status+")"})}return null}).catch(error=>{tasklist_api_request_active=false;console.log("Get task list HTTP request failed",error);// in state, so that this actually reaches the screen. The "last updated" time is
// deliberately left where it was: it is what tells the user how stale the page is.
this.setState({tasklist_api_error:"Connection error"})}).then(data=>{if(tasklist_refresh_queued&&!tasklist_api_request_active){// something asked for a refresh while this request was in flight; this
// response predates whatever prompted that, so fetch again right away.
// The pause is re-checked at execution time (the tab can have been hidden
// since the refresh was queued); a dropped refresh is covered by the first
// unpaused poll.
tasklist_refresh_queued=false;setTimeout(()=>{if(!pollingPaused()){this.fetchData(false)}},0)}let statechanges=null;if(data===NOT_MODIFIED){// nothing changed server-side, but the poll did succeed, so the "last updated"
// line must still advance or the page looks stalled.
//
// The held copy is re-applied rather than assumed to be what is on screen:
// "unchanged" is a statement about the server's page, not about this
// component's results, and the two come apart whenever a navigation replaced
// results locally (setSingleTaskView filters the list down to one task). Going
// Back from a single task then left the queue page showing that one task with
// no paginator, and no poll could ever repair it, because every one of them
// answered 304 for a URL whose ETag really had not changed.
//
// Guarded like the 200 path below: if the user navigated while this request
// was in flight, this body belongs to the page they have already left.
const held=tasklist_pollcache.getBody(get_url);const restore=held!=null&&get_url==window.location.href&&this.state.results!==held.results?held:null;this.setState({...restore,tasklist_last_fetch_time:new Date});return}if(data!=null&&data.hasOwnProperty("results")){if(data.results.length==0&&new URL(window.location.href).searchParams.get("cursor")!=null){// page is empty. redirect to main page
this.updateCursor(null)}else{statechanges=data}}else if(data!=null&&data.hasOwnProperty("id")){// single task view doesn't put task data inside 'results' list,
// so we create a single-item results list
statechanges={results:[data],next:null,previous:null,pagefirsttaskposition:null,taskcount:null}}if(statechanges!=null){statechanges["tasklist_last_fetch_time"]=new Date;// keyed off get_url, not window.location.href: if the user navigated while
// this request was in flight those differ, and storing the old page's body
// under the new page's key would both show the wrong tasks on a later revisit
// and let an If-None-Match be sent for a page we do not actually hold
tasklist_fetchcache[get_url]=statechanges;// an If-None-Match is only worth sending once a rendered copy exists to fall
// back on, since a 304 carries no body
tasklist_pollcache.storeBody(get_url,statechanges);if(get_url==window.location.href){debug_log("Applying results from",get_url);// the flag goes on a copy: statechanges was just stored in both caches,
// and setting it on the shared object polluted every later re-application
// of the cached body — the eager pre-request restore and the 304 fallback
// would scroll an untouched page to the top on a routine poll
this.setState(scrolltotop?{...statechanges,scrollToTopAfterUpdate:true}:statechanges)}else{debug_log("Not applying results from",get_url,"location.href",window.location.href);return}}})}componentDidUpdate(){// the tab title used to keep saying "Task Queue" after a client-side navigation, because
// every pushState passed the title it already had. Derived from dataurl here so that it is
// right no matter which navigation path got us here.
const title=pageTitle(this.singleTaskViewTaskId(this.state.dataurl));if(document.title!=title){document.title=title}if(this.state.scrollToTopAfterUpdate){this.setState({scrollToTopAfterUpdate:false});window.scrollTo(0,0);window.dispatchEvent(new Event("resize"))}}componentDidMount(){this.fetchinterval=pollInterval(()=>this.fetchData(false),TASKLIST_POLL_MS);this.queueposinterval=pollInterval(this.fetchQueuePositions,QUEUEPOS_POLL_MS);window.addEventListener("popstate",this.handlePopState);this.fetchData(true)}componentWillUnmount(){clearInterval(this.fetchinterval);clearInterval(this.queueposinterval);window.removeEventListener("popstate",this.handlePopState)}render(){const singletaskmode=this.singleTaskViewTaskId(this.state.dataurl)!=null;let pagehtml=[];if(!singletaskmode){pagehtml.push(/*#__PURE__*/React.createElement("div",{key:"header",className:"page-header"},/*#__PURE__*/React.createElement("h1",null,"Task Queue")))}else{pagehtml.push(/*#__PURE__*/React.createElement("div",{key:"header",className:"page-header"},/*#__PURE__*/React.createElement("h1",null,"Task ",this.singleTaskViewTaskId(this.state.dataurl))))}if(!singletaskmode||this.state.results!=null&&this.state.results.length>0&&this.state.results[0].user_id==user_id){// buttons rather than <a> without an href, which is not focusable and so could not be
// reached by keyboard at all. aria-pressed is what makes the selected one announceable.
pagehtml.push(/*#__PURE__*/React.createElement("ul",{key:"filters",id:"taskfilters"},/*#__PURE__*/React.createElement("li",{key:"all"},/*#__PURE__*/React.createElement("button",{type:"button",onClick:()=>this.setFilter(null),"aria-pressed":this.filterIsActive(null,this.state.dataurl),className:"btn "+this.filterclass(null,this.state.dataurl)},"All tasks")),/*#__PURE__*/React.createElement("li",{key:"started"},/*#__PURE__*/React.createElement("button",{type:"button",onClick:()=>this.setFilter("started"),"aria-pressed":this.filterIsActive("started",this.state.dataurl),className:"btn "+this.filterclass("started",this.state.dataurl)},"Running/Finished"))))}pagehtml.push(/*#__PURE__*/React.createElement(RunnerStatus,{key:"runnerstatus"}));if(this.state.tasklist_last_fetch_time!=null){pagehtml.push(/*#__PURE__*/React.createElement("p",{key:"tasklistfetchstatus",id:"tasklistfetchstatus"},"Last updated: ",this.state.tasklist_last_fetch_time.toLocaleString()," ",/*#__PURE__*/React.createElement("span",{className:"errors"},this.state.tasklist_api_error)))}if(!singletaskmode){const allow_stack_rock=new URL(this.state.dataurl).searchParams.get("allow_stack_rock")=="true";pagehtml.push(/*#__PURE__*/React.createElement(NewRequest,{key:"newrequest",fetchData:this.fetchData,allow_stack_rock:allow_stack_rock}))}let tasklist;if(this.state.results==null){tasklist=/*#__PURE__*/React.createElement("p",{key:"message"},"Loading tasks...")}else if(this.state.results.length==0){tasklist=/*#__PURE__*/React.createElement("p",{key:"message"},"There are no tasks.")}else{const pagetaskcount=this.state.results!=null?this.state.results.length:null;tasklist=[/*#__PURE__*/React.createElement("ul",{key:"ultasklist",className:"tasks"},this.state.results.map(task=>/*#__PURE__*/React.createElement(Task,{key:task.id,taskdata:task,fetchData:this.fetchData,setSingleTaskView:this.setSingleTaskView,hidePlot:pagetaskcount>10}))),/*#__PURE__*/React.createElement(Pager,{key:"pager",previous:this.state.previous,next:this.state.next,pagefirsttaskposition:this.state.pagefirsttaskposition,pagetaskcount:pagetaskcount,taskcount:this.state.taskcount,updateCursor:this.updateCursor})]}pagehtml.push(/*#__PURE__*/React.createElement("div",{key:"tasklist",id:"tasklist",className:singletaskmode?"singletaskdetail":null},tasklist));return pagehtml}}const container=document.getElementById("taskpage");const root=ReactDOM.createRoot(container);root.render(/*#__PURE__*/React.createElement(TaskPage,null));
