# rename this file to .env after editing

export ATLASSERVER_DJANGO_SECRET_KEY =''

# to generate a new key run:
# python3 manage.py shell -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"

export ATLASSERVER_EMAIL_HOST_USER='user@server.com'
export ATLASSERVER_EMAIL_HOST_PASSWORD='PASSWORD'

export ATLASSERVER_DJANGO_MYSQL_DBNAME='atlasserver'
export ATLASSERVER_DJANGO_MYSQL_USER='djangouser'
export ATLASSERVER_DJANGO_MYSQL_PASSWORD='PASSWORD'

export MAXMIND_LICENSE_KEY=''

export ATLASSERVER_NPROCESSES='4'
export ATLASSERVER_NTHREADPERPROC='1'

# Only needed when the checkout's own path contains a space, which the apachectl script generated
# by mod_wsgi-express cannot handle. Symlink the checkout somewhere without one and point this at
# the symlink; atlaswebserver refuses to start rather than fail later with an unrelated error.
# export ATLASSERVER_PATH='/Users/example/atlasserver'

# Number of reverse proxies in front of this server that maintain X-Forwarded-For. Leave at 0
# unless one really is in front: with 0 the header is ignored and the client address is taken from
# the connection, which is what stops a caller choosing the country their submissions are filed
# under. Count the proxies exactly — one hop too many reads a value the client wrote.
export ATLASSERVER_TRUSTED_PROXY_COUNT='0'

# Origin used to build the links in account-verification and email-change mail. Set this in
# production: without it the links are built from the request's Host header, and ALLOWED_HOSTS
# holds wildcard entries (.qub.ac.uk, .fallingstar-data.com), so anyone able to serve one of those
# subdomains could have the server mail a victim a link pointing at them — and be handed the
# victim's token when it is opened. Leave it unset in development, where the request is what you
# want. Must include the scheme.
export ATLASSERVER_SITE_ORIGIN='https://fallingstar-data.com'

# The standing note in the box above the content, on every page: a caveat about the data. Leave it
# empty or unset for no note. Inline HTML such as <strong> or a link is fine; a block element is
# not, because the note sits inside a <p> and the browser would close the paragraph in front of
# it. A change reaches the site with the restart that applies the new .env. Double quotes, not
# single: the apostrophe in the value ends a single-quoted string, and python-dotenv skips a line
# it cannot parse — the note would silently vanish.
export ATLASSERVER_SITE_NOTICE="Forced photometry is now available from the Southern Telescopes (El Sauce, Chile and Sutherland, South Africa). Please be aware that the difference imaging template south of -50 degrees declination was changed during commissioning, so you may get an unexpected discontinuity in your target's difference lightcurve."
